"""Geodesic Mycelium Agent package.

A minimal, dependency-light agent scaffold that:
- Loads the canonical progression doc and any local docs index.
- Exposes a CLI command to run an agent loop in dry-run mode.
- Defines clear extension points for LLM providers, MCP tool-calling,
  and persistent memory backends.
"""

__version__ = "0.1.0"
