# SymCrucible Transition Console v0.2 — Replication Receipt

This receipt is for independent second-party replication of the browser-native demo instrument.

## Artifact

```text
Name: SYMCRUCIBLE_TRANSITION_CONSOLE_v0.2
Repository: ryansctt1994-sudo/Ouroboros
Upstream anchor commit: 88a8ce0b3346b7422ef417f024db5f45ccae9f5c
Status: DEMO_INSTRUMENT
Authority: TIER_UI / AUTHORITY_WEIGHT_0
Boundary: not a production verifier, not a replicated receipt system, not an authority kernel
```

## Replicator

```text
Name / handle:
Organization:
Date UTC:
Machine / OS:
Browser:
Git version:
Python version:
```

## Procedure

```bash
git clone https://github.com/ryansctt1994-sudo/Ouroboros.git
cd Ouroboros
bash run.sh
```

## Checks

Mark each result.

```text
[ ] Required files present
[ ] MANIFEST.sha256 upstream blob anchors verified
[ ] REPLICATION_OBSERVED.sha256 generated
[ ] Local server opened at http://localhost:8000/
[ ] Console loaded successfully
[ ] Default deck loaded or manually loaded
[ ] Default policy loaded or manually loaded
[ ] Scenario sweep exported
[ ] Chronicle exported
[ ] Receipt bundle exported
[ ] Tamper test run
[ ] Verify chain detects tamper
[ ] Replay chronicle succeeds on untampered export
[ ] Replay chronicle fails on manually altered export
```

## Observed result

```text
PASS / FAIL:
Notes:
```

## Observed SHA-256 manifest

Attach or paste the contents of `REPLICATION_OBSERVED.sha256` below.

```text

```

## Signature

```text
Replicator signature / attestation:
```
